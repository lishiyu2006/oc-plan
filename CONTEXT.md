# OC PLAN(AETHERION 世界观展示站)

一个展示作者原创角色(OC)及其世界观 AETHERION(艾瑟里昂)的静态展示站;内容以 JSON 驱动,页面围绕 3D 世界观大陆、人物画廊与记录展开。

## 内容与展示

**OC / 角色 (character)**:
内容库中的一个人物(`characters.json` 条目),拥有立绘与插画集。站点围绕它提供人物详情与记录两个展示形态。
_Avoid_: 用户、作者

**立绘 (photo)**:
角色的主视觉竖图,用于人物详情页左半屏展示。
_Avoid_: 头像、图

**插画 (illustration)**:
角色相关的画廊图片集,用于记录页。
_Avoid_: 相片、画廊图

**记录 (record)**:
某个角色的插画展示形态(/records/:id 路由,画廊 + lightbox),与人物详情(/characters/:id)互为补充。
_Avoid_: 相册、日记

## 世界观(3D 大陆)

**世界观 (world)**:
/ 页展示的 AETHERION 3D 大陆整体,由一个可旋转浏览、分层切换的 3D 场景呈现。
_Avoid_: 地图、场景页

**板块 (region)**:
大陆上一处有名称、有介绍的命名区域(`world.json` 条目),是浏览与介绍的基本单位;用户在 3D 中点击其展示实体进入介绍面板。
_Avoid_: 标记、点位、坐标、地标、marker

**层 (layer)**:
大陆在纵向(高度)上的归属,分 `sky`(天空)/ `surface`(地表)/ `underground`(地下)三层;每个板块属于且仅属于一层,层决定板块在空间中的上下位置与浏览视角。
_Avoid_: 场景、层级视图

**昼夜 (day/night)**:
站点的明暗主题与 3D 大陆光照的联动——亮色主题 = 大陆白天,暗色主题 = 大陆黑夜;天空/地表层随主题变化,地下层光照恒定。
_Avoid_: 主题色、光照模式

## 3D 资产

**场景模型 (scene model)**:
大陆 3D 资产的单位:一个按"渲染场景"切分的 glTF 文件(`top` 含天空+地表、`under` 含地下),由建模者产出、管理员上传,经图床以 URL 引用。
_Avoid_: 场景文件、model 节点、mesh

**板块展示实体 (region landmark)**:
板块在场景模型中可被点击的 3D 实体,与板块 id 同名,是建模者在场景里搭出的地标本体(而非前端生成的通用标记)。
_Avoid_: 宝石、浮标、marker、gem

**锚点 (anchor)**:
场景模型中与板块 id 同名的命名节点,标定板块展示实体在 3D 空间中的位置与可点击范围;前端凭名字在场景模型里定位板块。
_Avoid_: 空物体、标记点、坐标点

## 内容与资产管线

**内容数据 (content data)**:
站点的展示内容来源——仓库根 `content/` 下的 JSON(世界/人物)+ 图片,由内容管理员维护,经 git 同步发布。
_Avoid_: 配置、数据文件、后端数据

**图床 (image host)**:
存放并对外提供内容图片与 3D 模型文件的 GitHub 仓库(lishiyu2006/picgo),经 jsDelivr CDN 以 URL 引用;管理员通过上传流程将资产登记为图床 URL,写回内容数据。
_Avoid_: 服务器、云盘、静态目录
