## Agent skills

### Issue tracker

Issues live in GitHub Issues for `lishiyu2006/oc-plan`; use the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical roles map to the labels `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.

### three.js 代码规范

仓库内所有 three.js / 3D 场景代码规范见 [`threejs-world-architecture.md`](./docs/threejs-world-architecture.md)。
